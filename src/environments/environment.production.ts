export const environment = {
  production: true,
  // Updated after backend Vercel deploy — override via replace if needed
  apiBaseUrl: 'https://bakery-backend.vercel.app/api',
  assetsBaseUrl: 'https://bakery-backend.vercel.app',
};
